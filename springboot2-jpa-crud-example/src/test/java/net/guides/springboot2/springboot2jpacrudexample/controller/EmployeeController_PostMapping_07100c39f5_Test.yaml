# Test generated by RoostGPT for test demo5 using AI Type Azure Open AI and AI Model roost-gpt

config:
  target: "http://dummyapi.com" # replace with the actual API endpoint
  phases:
    - duration: 5 # test duration in seconds
      arrivalRate: 1 # number of requests per second

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
          json:
            name: "John Smith"
            age: 30
            department: "IT"
      - expect:
          statusCode: 200
          json:
            name: "John Smith"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
          json:
            name: "John Smith"
      - expect:
          statusCode: 400
          json:
            errorMessage: "Missing required field: age"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
          json:
            name: "John Smith"
            age: 30
            department: "IT"
            data: "<large data or special characters>"
      - expect:
          statusCode:
            - 200
            - 201
          json:
            data: "<expected pattern or behavior>"
