# Test generated by RoostGPT for test demo56 using AI Type Azure Open AI and AI Model roost-gpt


  Please regenerate the following artillery test case, as the generated yaml file is not a valid yaml due to following error :
  ERRORS:
  config:
  target: "https://dummy-api-endpoint.com"
  phases:
    - duration: 30
      arrivalRate: 10
  defaults:
    headers:
      Content-Type: application/json

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/employees"
          json:
            name: "John Doe"
            age: 30
            role: "Software Engineer"
          capture:
            - json: "$.id"
              as: "employeeId"
      - get:
          url: "/employees"
      - validate:
          - status: 200
          - body: 
              - expect: "$[0].id"
                equal: "{{ employeeId }}"
              - exist: "$[0].name"
              - exist: "$[0].age"
              - exist: "$[0].role"
  
  - name: "API request with validation failure"
    flow:
      - post:
          url: "/employees"
          json:
            name: "John Doe"
          capture:
            - json: "$.message"
              as: "errorMessage"
      - validate:
          - status: 400
          - body:
              - expect: "$.message"
                equal: "Missing required fields"
  
  - name: "API request with edge case"
    flow:
      - post:
          url: "/employees"
          json:
            name: "John Doe with special characters @_@#$%"
            age: 100
            role: "A".repeat(10000)
      - get:
          url: "/employees"
      - validate:
          - status:
              min: 200
              max: 299
          - body:
              - match: "$[0].role"
                pattern: "A{10000}"

  TEST CASE:
  