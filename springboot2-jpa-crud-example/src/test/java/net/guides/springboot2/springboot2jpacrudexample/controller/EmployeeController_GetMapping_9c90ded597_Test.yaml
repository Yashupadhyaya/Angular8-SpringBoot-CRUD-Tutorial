# Test generated by RoostGPT for test demo5 using AI Type Azure Open AI and AI Model roost-gpt

config:
  target: "http://dummy-api.com"

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/employees"
          json:
            name: "John Doe"
            age: 30
            position: "Manager"
          headers:
            Content-Type: "application/json"
      - expect:
          statusCode: 200
          body:
            contains:
              - "John Doe"
              - 30
              - "Manager"
    
  - name: "API request with validation failure"
    flow:
      - post:
          url: "/employees"
          json:
            name: ""
            age: 25
            position: "Developer"
          headers:
            Content-Type: "application/json"
      - expect:
          statusCode: 400
          body:
            contains:
              - "Validation failed: Name is required."
    
  - name: "API request with edge case"
    flow:
      - post:
          url: "/employees"
          json:
            name: "Jane Doe"
            age: 35
            position: "${randomSpecialCharacters}"
          headers:
            Content-Type: "application/json"
      - expect:
          statusCode: { from: 200, to: 299 }
          body:
            matches: ".*Jane.*"
            notMatches: ".*Doe.*"
