# Test generated by RoostGPT for test demo5 using AI Type Azure Open AI and AI Model roost-gpt

config:
  target: "http://dummy-api-endpoint.com"

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
          json:
            name: "John Doe"
            age: 30
            salary: 5000
      - expect:
          statusCode: 200
          jsonEquals:
            - path: "$.[0].name"
              value: "John Doe"
            - path: "$.[0].age"
              value: 30
            - path: "$.[0].salary"
              value: 5000

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
          json:
            name: "Jane Smith"
      - expect:
          statusCode: 400
          jsonEquals:
            - path: "$.error"
              value: "Missing required fields"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
          json:
            name: "Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed consectetur sagittis metus."
      - expect:
          statusCode:
            min: 200
            max: 500
          json:
            - path: "$.[0].name"
              matches: "Lorem ipsum.+"
