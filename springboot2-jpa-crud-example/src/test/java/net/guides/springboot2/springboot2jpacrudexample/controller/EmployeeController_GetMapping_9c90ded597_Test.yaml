# Test generated by RoostGPT for test demo5 using AI Type Azure Open AI and AI Model roost-gpt

config:
  target: "http://dummy-api-endpoint.com"
  phases:
    - duration: 5
      arrivalRate: 1

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/employees"
          json:
            name: "John Doe"
            age: 30
          headers:
            Content-Type: "application/json"
        expect:
          statusCode: 201
          json:
            id: "1"
            name: "John Doe"
            age: 30

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/employees"
          json:
            age: 25
          headers:
            Content-Type: "application/json"
        expect:
          statusCode: 400
          json:
            error: "Invalid payload. Missing required field 'name'."

  - name: "API request with edge case"
    flow:
      - post:
          url: "/employees"
          json:
            name: "Lorem Ipsum"
            age: 100
          headers:
            Content-Type: "application/json"
        expect:
          statusCode: { from: 200, to: 204 }
          json:
            id: "{{ $randomNumber }}"
            name: "Lorem Ipsum"
            age: 100
