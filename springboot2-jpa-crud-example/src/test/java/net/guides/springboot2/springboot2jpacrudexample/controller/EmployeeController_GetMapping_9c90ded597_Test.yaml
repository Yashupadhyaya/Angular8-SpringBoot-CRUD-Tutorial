# Test generated by RoostGPT for test demo5 using AI Type Azure Open AI and AI Model roost-gpt

config:
  target: "http://dummy-api-endpoint.com"

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
          json:
            name: "John Doe"
            age: 25
        expect:
          - statusCode: 201
          - json:
              name: "John Doe"
              age: 25

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
          json:
            name: "Jane Smith"
        expect:
          - statusCode: 400
          - json:
              error: "Required field 'age' is missing"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
          json:
            name: "!@#$%^&*()"
            age: 150
        expect:
          - statusCode: { from: 200, to: 400 }
          - jsonPattern: {"name": "[A-Za-z0-9]+", "age": [0-9]+}
